import styled from "styled-components";

// Composant représentant le style d'une grille de boutons
const ButtonGrid = styled.div`
  display: grid;
  grid-template-columns: 1fr 1fr 1fr 1fr;
  gap: .5em;
`

export default ButtonGrid;
